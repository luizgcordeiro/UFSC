import numpy as np
import sys
from io import StringIO

old_stdout=sys.stdout
sys.stdout=StringIO()

XX=np.random.randint(-100,100)
YY=np.random.randint(-100,100)
sys.stdin = StringIO(str(XX)+"\n"+str(YY))
SS=XX+YY

import soma

sys.stdout=old_stdout

if (soma.S==SS):
    print("Sucesso.")
else:
    print("Seu programa falhou.")
#end if-else